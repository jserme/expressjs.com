section
  h3(id='express') express()
  
  p.
    创建一个express应用程序

  +js.
    var express = require('express');
    var app = express();
    
    app.get('/', function(req, res){
      res.send('hello world');
    });
    
    app.listen(3000);
