section
  h3(id='app.disabled') app.disabled(name)

  p.
    检查设置项 <code>name</code> 是不是非激活状态

  +js.
    app.disabled('trust proxy');
    // => true

    app.enable('trust proxy');
    app.disabled('trust proxy');
    // => false
