section
  h3(id='res.set') res.set(field, [value])
  p 
    设置响应头字段<code>field</code> 值为 <code>value</code>,
    也可以一次传入一个对象设置多个值。

  +js.
    res.set('Content-Type', 'text/plain');

    res.set({
      'Content-Type': 'text/plain',
      'Content-Length': '123',
      'ETag': '12345'
    })

  p <code>res.header(field, [value])</code>的别名。
